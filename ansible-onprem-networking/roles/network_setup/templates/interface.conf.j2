# Network interface configurations with Jinja2
auto eth0
iface eth0 inet static
    address {{ ip_address }}
    netmask {{ netmask }}
    gateway {{ gateway }}
    dns-nameservers {% for dns in dns_servers %}{{ dns }} {% endfor %}

